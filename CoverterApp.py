class ConverterApp:
    def main(self):
        filename = input('Select 1 for Moarse to English and Select 2 for English to Moarse')
        message = "GEEKS-FOR-GEEKS"
        result = encrypt(message.upper())
